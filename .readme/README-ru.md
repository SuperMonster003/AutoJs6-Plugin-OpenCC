<!--suppress HtmlDeprecatedAttribute, HttpUrlsUsage -->

<div align="center">
  <p>
    <picture>
      <source srcset="https://github.com/SuperMonster003/AutoJs6-Plugin-OpenCC/blob/master/app/src/main/res/mipmap-night/ic_launcher.png?raw=true" media="(prefers-color-scheme: dark)" />
      <img src="https://github.com/SuperMonster003/AutoJs6-Plugin-OpenCC/blob/master/app/src/main/res/mipmap/ic_launcher.png?raw=true" alt="autojs6-plugin-opencc-ic-launcher" border="0" width="128" />
    </picture>
  </p>

  <p>Автономный офлайн-конвертер OpenCC для китайского текста и AutoJs6</p>

  <p>
    <a href="https://github.com/SuperMonster003/AutoJs6-Plugin-OpenCC/releases"><img alt="GitHub release (latest by date)" src="https://img.shields.io/github/v/release/SuperMonster003/AutoJs6-Plugin-OpenCC?label=Release"/></a>
    <a href="https://github.com/SuperMonster003/AutoJs6-Plugin-OpenCC/issues"><img alt="GitHub closed issues" src="https://img.shields.io/github/issues/SuperMonster003/AutoJs6-Plugin-OpenCC?color=A24232&label=Issues"/></a>
    <a href="https://github.com/SuperMonster003/AutoJs6-Plugin-OpenCC/blob/master/LICENSE"><img alt="GitHub License" src="https://img.shields.io/github/license/SuperMonster003/AutoJs6-Plugin-OpenCC?color=534BAE&label=License"/></a>
  </p>
</div>

******

### Языки (Languages)

******

Текущий README.md поддерживает следующие языки:

- [简体中文 [zh-Hans]](https://github.com/SuperMonster003/AutoJs6-Plugin-OpenCC/blob/master/.readme/README-zh-Hans.md)
- [繁體中文 (香港) [zh-Hant-HK]](https://github.com/SuperMonster003/AutoJs6-Plugin-OpenCC/blob/master/.readme/README-zh-Hant-HK.md)
- [繁體中文 (台灣) [zh-Hant-TW]](https://github.com/SuperMonster003/AutoJs6-Plugin-OpenCC/blob/master/.readme/README-zh-Hant-TW.md)
- [English [en]](https://github.com/SuperMonster003/AutoJs6-Plugin-OpenCC/blob/master/.readme/README-en.md)
- [Français [fr]](https://github.com/SuperMonster003/AutoJs6-Plugin-OpenCC/blob/master/.readme/README-fr.md)
- [Español [es]](https://github.com/SuperMonster003/AutoJs6-Plugin-OpenCC/blob/master/.readme/README-es.md)
- [日本語 [ja]](https://github.com/SuperMonster003/AutoJs6-Plugin-OpenCC/blob/master/.readme/README-ja.md)
- [한국어 [ko]](https://github.com/SuperMonster003/AutoJs6-Plugin-OpenCC/blob/master/.readme/README-ko.md)
- Русский [ru] # текущий
- [العربية [ar]](https://github.com/SuperMonster003/AutoJs6-Plugin-OpenCC/blob/master/.readme/README-ar.md)

******

### Введение

******

OpenCC предоставляет в одной установке два способа преобразования китайского текста на основе [OpenCC](https://github.com/BYVoid/OpenCC). Запускайте полностью автономное Android-приложение либо позвольте AutoJs6 распознать тот же APK как плагин и используйте глобальный объект `opencc` в скриптах. Оба пути охватывают упрощенный и традиционный китайский, варианты Гонконга и Тайваня и японский синдзитай.

Автономный редактор и защищенная разрешением служба Binder для AutoJs6 используют один официальный движок OpenCC, одинаковые закрепленные словари, кэш, типы преобразования и модель ошибок. Приложению AutoJs6 не нужен, а режим плагина сохраняет существующий API скриптов и позволяет обновлять движок независимо от хоста.

******

### Основные возможности

******

- Один APK, два режима: открывайте значок запуска для визуального преобразования без AutoJs6 либо используйте ту же установку через скриптовый API `opencc` в AutoJs6.
- 14 стандартных преобразований: охватывают преобразование OpenCC между упрощенным и традиционным китайским, гонконгские и тайваньские варианты и японский синдзитай, включая преобразование тайваньской лексики (например, взаимную замену `软件` и `軟體`).
- 33 скриптовых метода: помимо универсального `opencc.convert(text, type)`, у каждого типа преобразования есть одноименный сокращенный метод, а также 18 методов-псевдонимов и составных методов, таких как `s2jp` и `tw2hk`.
- Полностью офлайн: преобразование выполняется локально на встроенных словарях плагина; плагин не запрашивает сетевое разрешение и не собирает никаких данных.
- Пакеты по потребностям: 4 пакета для одной ABI и пакет `universal` со всеми ABI, поэтому каждое устройство устанавливает только необходимое.
- Многоязычность: автономный интерфейс, сведения о плагине, инструкции, README и журнал изменений охватывают 10 языков.
- Общий backend: редактор и легковесная служба повторно используют одинаковые проверенные ресурсы и нативный движок; неиспользуемые подключения освобождаются автоматически.

******

### Снимок интерфейса

******

Необработанные снимки Android показывают автономный редактор в светлой теме, арабскую RTL-компоновку с размером шрифта 170% в темной теме и существующую запись в центре плагинов AutoJs6.

<table>
  <tr>
    <td align="center">
      <img src="https://github.com/SuperMonster003/AutoJs6-Plugin-OpenCC/blob/master/docs/images/screenshots/standalone-phone-light.png?raw=true"
           alt="Автономное офлайн-преобразование в светлой теме" width="280" />
      <br />
      <sub>Автономное офлайн-преобразование в светлой теме</sub>
    </td>
    <td align="center">
      <img src="https://github.com/SuperMonster003/AutoJs6-Plugin-OpenCC/blob/master/docs/images/screenshots/standalone-rtl-large-dark.png?raw=true"
           alt="Арабская RTL-компоновка, шрифт 170%, темная тема" width="280" />
      <br />
      <sub>Арабская RTL-компоновка, шрифт 170%, темная тема</sub>
    </td>
    <td align="center">
      <img src="https://github.com/SuperMonster003/AutoJs6-Plugin-OpenCC/blob/master/docs/images/screenshots/plugin-center-enabled.png?raw=true"
           alt="OpenCC 1.0.2 распознан и включен в центре плагинов" width="280" />
      <br />
      <sub>OpenCC 1.0.2 распознан и включен в центре плагинов</sub>
    </td>
  </tr>
</table>

******

### Использование

******

1. Скачайте и установите один APK со страницы [Releases](https://github.com/SuperMonster003/AutoJs6-Plugin-OpenCC/releases) или из центра плагинов AutoJs6. Выберите пакет для ABI устройства; если сомневаетесь, используйте `universal` или раздел `Как выбрать установочный пакет`.
2. Для автономной работы откройте `OpenCC` из лаунчера, введите или явно вставьте текст, выберите один из 14 типов и нажмите `Преобразовать`. AutoJs6 и выдача разрешения плагина не требуются.
3. Для режима плагина обновите AutoJs6 до внутренней сборки 3923 (6.7.1 Alpha4) или новее; выпуск 6.8.0 и последующие удовлетворяют требованию.
4. В центре плагинов AutoJs6 убедитесь, что `OpenCC` распознан и включен. Официальные пакеты автоматически проходят проверку подписи без ручной авторизации.
5. Используйте глобальный объект `opencc` прямо в скриптах, например `opencc.s2t("汉字")`; require, import и перезапуск хоста не нужны.

> Оба режима поддерживают Android 7.0 (API 24) и выше. Минимальная сборка AutoJs6 относится только к скриптам плагина; автономное приложение от хоста не зависит. Если скрипт сообщает об отсутствии плагина или старом хосте, см. `Частые вопросы`.

******

### Быстрый старт

******

После установки следующий скрипт запускается как есть; в комментариях показан ожидаемый вывод:

```javascript
console.log(opencc.s2t("汉字转换"));     // => 漢字轉換
console.log(opencc.t2s("漢字轉換"));     // => 汉字转换
console.log(opencc.s2twp("鼠标和软件")); // => 滑鼠和軟體
console.log(opencc.t2jp("圖書館"));      // => 図書館
```

Сокращенные методы эквивалентны универсальному методу `opencc.convert(text, type)`; сам объект `opencc` также можно вызывать как функцию, а имена типов преобразования не зависят от регистра:

```javascript
console.log(opencc.convert("汉字转换", "S2T")); // => 漢字轉換
console.log(opencc("汉字转换", "s2t"));         // => 漢字轉換
```

Все методы синхронно возвращают преобразованную строку; преобразование выполняется на локальных словарях и никогда не обращается к сети.

******

### Типы преобразования

******

Метод `convert` и одноименные сокращенные методы поддерживают следующие 14 стандартных типов преобразования OpenCC, где S означает упрощенный китайский, T — традиционный (стандарт OpenCC), HK — гонконгский традиционный, TW — тайваньский традиционный, а JP — японский синдзитай:

| Тип | Направление |
|---|---|
| `S2T` | Из упрощенного в традиционный |
| `T2S` | Из традиционного в упрощенный |
| `S2TW` | Из упрощенного в тайваньский традиционный |
| `TW2S` | Из тайваньского традиционного в упрощенный |
| `S2TWP` | Из упрощенного в тайваньский традиционный с заменой на тайваньскую лексику (например, `内存` становится `記憶體`) |
| `TW2SP` | Из тайваньского традиционного в упрощенный с заменой на лексику материкового Китая (например, `滑鼠` становится `鼠标`) |
| `S2HK` | Из упрощенного в гонконгский традиционный |
| `HK2S` | Из гонконгского традиционного в упрощенный |
| `T2TW` | Из традиционного в тайваньский традиционный |
| `TW2T` | Из тайваньского традиционного в традиционный |
| `T2HK` | Из традиционного в гонконгский традиционный |
| `HK2T` | Из гонконгского традиционного в традиционный |
| `T2JP` | Из традиционного (кюдзитай) в японский синдзитай |
| `JP2T` | Из японского синдзитай в традиционный (кюдзитай) |

Типы с суффиксом `P` помимо познакового преобразования выполняют и замену лексики, поэтому результат звучит естественно для местных читателей; типы без `P` преобразуют только формы иероглифов, не затрагивая лексику.

`T2JP` и `JP2T` преобразуют между традиционными старыми формами иероглифов (кюдзитай) и японским синдзитай, например `圖書館` и `図書館`; они работают с различиями в формах иероглифов и не являются переводом между китайским и японским языками.

******

### Скриптовые методы

******

Глобальный объект `opencc` на стороне хоста предоставляет в общей сложности 33 метода: универсальный метод `convert`, 14 основных сокращенных методов и 18 методов-псевдонимов и составных методов. Аргумент `type` метода `convert(text, type)` принимает все 32 имени преобразований (как основных, так и составных) без учета регистра; передача неизвестного типа вызывает ошибку `Unknown OpenCC conversion type`.

14 основных сокращенных методов взаимно однозначно соответствуют типам преобразования из таблицы выше; каждый вызов выполняет одно преобразование в плагине:

```text
s2t   t2s   s2tw  tw2s  s2twp  tw2sp  s2hk
hk2s  t2tw  tw2t  t2hk  hk2t   t2jp   jp2t
```

`s2twi` и `twi2s` являются псевдонимами `s2twp` и `tw2sp` соответственно (`twi` означает Taiwan idiom, то есть тайваньскую лексику) и ведут себя идентично.

Остальные 16 составных методов последовательно объединяют несколько основных преобразований и покрывают направления, для которых нет прямого словаря:

```text
s2jp   = s2t  + t2jp          jp2s   = jp2t + t2s
hk2tw  = hk2t + t2tw          tw2hk  = tw2t + t2hk
hk2jp  = hk2t + t2jp          tw2jp  = tw2t + t2jp
t2twi  = t2s  + s2twi         twi2t  = twi2s + s2t
hk2twi = hk2s + s2twi         twi2hk = twi2s + s2hk
tw2twi = tw2s + s2twi         twi2tw = twi2s + s2tw
jp2hk  = jp2t + t2hk          jp2tw  = jp2t + t2tw
twi2jp = twi2s + s2t + t2jp   jp2twi = jp2t + t2s + s2twi
```

Новый хост с поддержкой расширенного контракта передает всю составную цепочку одним вызовом плагина; например, для 3 этапов `twi2jp` нужен только 1 обмен Binder. Старые хосты продолжают вызывать каждый этап и сохраняют совместимость с этим плагином.

******

### Как выбрать установочный пакет

******

Каждый выпуск включает 5 APK, которые различаются только тем, какие архитектуры процессора (ABI) нативной библиотеки OpenCC они содержат:

| Пакет | Кому подходит |
|---|---|
| `arm64-v8a` | Подавляющее большинство современных телефонов и планшетов Android (64-битный ARM); выбирайте в первую очередь |
| `armeabi-v7a` | Более старые 32-битные устройства ARM |
| `x86_64` | 64-битные эмуляторы x86 и немногочисленные устройства x86 |
| `x86` | 32-битные эмуляторы x86 и немногочисленные устройства x86 |
| `universal` | Содержит все 4 архитектуры и имеет наибольший размер; работает на любом устройстве и остается надежным выбором, если вы не уверены |

Если по ошибке установлен пакет для одной ABI, не соответствующей архитектуре устройства, плагин не сможет выполнять преобразование; установка пакета `universal` решает проблему.

******

### Быстрая самопроверка

******

Убедившись, что плагин установлен и включен в центре плагинов, выполните этот однострочный скрипт для сквозной проверки:

```javascript
console.log(opencc.s2t("汉字转换"));
```

Вывод `漢字轉換` означает, что вся цепочка плагина работает. Если скрипт завершается ошибкой, действуйте по сообщению: установите этот плагин, если сообщается об отсутствии плагина; включите соответствующий переключатель в центре плагинов, если плагин отключен или не авторизован; обновите AutoJs6, если требуется более новый хост.

******

### Частые вопросы

******

#### Как убедиться, что плагин работает?

Откройте центр плагинов AutoJs6: если плагин `OpenCC` виден в списке и включен, хост его распознал. Затем запустите скрипт из раздела `Быстрая самопроверка` выше; вывод `漢字轉換` подтверждает работоспособность.

#### Можно ли использовать OpenCC без установки AutoJs6?

Да. Откройте значок `OpenCC` и преобразуйте текст в автономном редакторе. AutoJs6 нужен только тогда, когда скрипт вызывает плагин через глобальный объект `opencc`; оба режима входят в один APK.

#### Скрипт сообщает `Отсутствует необходимый плагин для "OpenCC plugin"`. Что делать?

Это означает, что AutoJs6 не нашел плагин на устройстве. Установите плагин и запустите скрипт снова; перезапуск AutoJs6 не нужен. Если после установки сообщение сохраняется, убедитесь, что плагин не был удален системой или защитным приложением, и проверьте его состояние включения и авторизации в центре плагинов.

#### В чем разница между `s2tw` и `s2twp` (`s2twi`)?

`s2tw` преобразует только формы иероглифов (например, `软` становится `軟`) и не меняет лексику; `s2twp` дополнительно заменяет материковую лексику тайваньской (например, `软件` становится `軟體`, а `鼠标` становится `滑鼠`), а `s2twi` является его псевдонимом. Для текстов, адресованных тайваньским читателям, предпочтителен `s2twp`; когда нужно лишь унифицировать формы иероглифов, выбирайте `s2tw`.

#### Почему `opencc` недоступен в скриптах на движке Node.js?

`opencc` пока доступен только в Rhino, JavaScript-движке AutoJs6 по умолчанию; среда выполнения Node.js соответствующей реализации еще не предоставляет. Связанные планы см. в [ROADMAP.md](https://github.com/SuperMonster003/AutoJs6-Plugin-OpenCC/blob/master/ROADMAP.md).

#### Нужна ли для преобразования сеть? Медленно ли преобразуется длинный текст?

Сеть не нужна; все преобразования выполняются локально на словарях OpenCC, встроенных в плагин. Каждый вызов метода соответствует одному межпроцессному обмену, и даже длинные тексты обычно преобразуются за один обмен; в высокочастотных циклах предпочитайте основные типы, чтобы избежать лишних обменов, которые добавляют составные методы.

#### Какие разрешения запрашивает плагин? В безопасности ли мои данные?

Плагин объявляет только разрешение плагина, используемое для связи с AutoJs6, и не запрашивает чувствительных системных разрешений, таких как сеть или хранилище; его служба защищена тем же разрешением, поэтому другие приложения не могут ее вызвать. Преобразуемый текст остается в памяти устройства и никогда не сохраняется и не выгружается.

******

### Разрешения и безопасность

******

У автономного приложения и входа плагина AutoJs6 разные явные границы:

- Минимальные разрешения: манифест объявляет лишь `org.autojs.permission.PLUGIN` для интеграции, без чувствительных разрешений сети, хранилища или камеры; автономный пользователь не выдает это разрешение.
- Явные действия: Launcher не принимает общий текст или URI, читает буфер обмена только после `Вставить` и открывает системную панель только после `Поделиться`.
- Защищенная служба: привязать и вызвать ее могут только хосты с разрешением, например AutoJs6. AutoJs6 также проверяет подпись пакета; другие приложения вызвать службу не могут.
- Локальная обработка: оба входа используют встроенные словари полностью офлайн. Ввод и результат не журналируются, не сохраняются, не резервируются, не отправляются и не собираются.

Получайте плагин только с официальной страницы [Releases](https://github.com/SuperMonster003/AutoJs6-Plugin-OpenCC/releases) или из центра плагинов AutoJs6. Пакеты из неизвестных источников могут не пройти проверку хоста или скрывать риски, даже если номер версии выглядит одинаково.

******

### Интерфейс плагина

******

Следующая информация предназначена для разработчиков хоста AutoJs6 и плагинов; по этим идентификаторам хост обнаруживает плагин и согласует совместимость:

```text
application id: io.github.supermonster003.autojs6.plugin.opencc
plugin id: opencc
engine: opencc
variant: default
service action: org.autojs.plugin.OPENCC
service category: opencc
aidl interface: org.autojs.plugin.opencc.api.IOpenccPlugin
aidl contract version: 2
aidl methods: getInfo(), convert(text, conversionType), getSupportedConversionTypes(), convertBatch(texts, conversionType), convertChain(text, conversionTypes)
batch/chain limits: 1024 texts / 32 stages
minimum host build: 3923 (6.7.1 Alpha4)
conversion backend: OpenCC 1.4.2 (ver.1.4.2)
OpenCC source commit: 025f371dc76b598d77384fbdab90c937471844d8
OpenCC resources SHA-256: 9ea0d303219b34d014d5c116677b5d325043beafb2c8a62ee889ca67f4d054a5
```

`OpenccPluginService` отвечает на действие `org.autojs.plugin.OPENCC` (категория `opencc`) через `org.autojs.plugin.opencc.api.IOpenccPlugin` из opencc-api. Версия контракта 2 добавляет обнаружение типов, пакетное и цепочечное преобразование после исходных методов `getInfo()` и `convert(text, conversionType)`, а также сообщает версию и поддерживаемые типы через `PluginInfo.capabilities`; старые хосты продолжают использовать исходные методы и номера транзакций. Также предоставляется `WakeActivity` для пробуждения процесса плагина.

Плагин напрямую собирает официальный OpenCC `ver.1.4.2` на коммите `025f371dc76b598d77384fbdab90c937471844d8` с ресурсами того же выпуска. Для каждой ABI включена только одна статически скомпонованная и выровненная на 16 KB библиотека `libopencc_jni.so`; преобразование остается полностью автономным.

******

### Дорожная карта

******

Планы и ход работ по плагину ведутся в ROADMAP.md в виде списка с отметками, сгруппированного по вехам с критериями приемки; список охватывает документацию и опыт выпуска, инженерную инфраструктуру и непрерывную интеграцию, расширение возможностей преобразования и развитие среды выполнения. Неотмеченные пункты выражают намерения, а не текущие возможности; обсуждение через Issues приветствуется.

- [Открыть ROADMAP.md](https://github.com/SuperMonster003/AutoJs6-Plugin-OpenCC/blob/master/ROADMAP.md)

******

### История выпусков

******

#### v1.2.0

_2026/09/01_

- `Подсказка` Обновление словарей OpenCC 1.4.2 намеренно меняет некоторые результаты, включая `复盘` -> `復盤`, `内卷` -> `內捲`, сохранение `什么怎么这么` и `内存条` -> `記憶體模組`; полный проверенный список приведен в отчете о миграции
- `Улучшение` Прямая сборка официального OpenCC 1.4.2 и словарей того же выпуска в одну статически скомпонованную библиотеку JNI для каждой ABI с сохранением полностью автономного преобразования
- `Улучшение` Поддержка устройств со страницами 16 KB за счет NDK 28.2, выравнивания ELF и ZIP на 16 KB и проверки Binder на настоящем эмуляторе 16 KB
- `Улучшение` Атомарная установка закрепленного ZIP с ресурсами и проверкой размера и SHA-256, автоматическим восстановлением после повреждения, безопасным для Unicode JNI и кешированием конвертеров горячего пути
- `Зависимость` Удаление неподдерживаемой обертки `com.github.brooklet:android-opencc:1.2.2` и фиксация официального OpenCC `ver.1.4.2` на коммите `025f371dc76b598d77384fbdab90c937471844d8`
- `Зависимость` Документирование источников и лицензий встроенных OpenCC, Marisa Trie, Darts Clone и RapidJSON в `THIRD_PARTY_NOTICES.md`

#### v1.1.0

_2026/09/01_

- `Функция` Обновление контракта плагина OpenCC до версии 2 с методом `getSupportedConversionTypes()`, чтобы новые хосты могли динамически определять 14 реально поддерживаемых типов преобразования
- `Функция` Добавление `convertBatch(texts, conversionType)` для преобразования до 1024 фрагментов текста за один обмен Binder с сохранением поэлементного пути для старых хостов
- `Функция` Добавление `convertChain(text, conversionTypes)` для последовательного выполнения до 32 этапов за один вызов, что сокращает составные методы в новых хостах с максимум 3 обменов Binder до 1
- `Улучшение` Передача локализованных инструкций через `PluginInfo.instruction` и публикация версии контракта и поддерживаемых типов преобразования через capabilities
- `Улучшение` Сохранение исходных методов AIDL и номеров транзакций, а также модульные и реальные Binder-тесты расширенных вызовов, отката к старому контракту, ограничений размера и ошибок
- `Улучшение` Унифицировать оформление README и управление версиями платформы Gradle

#### v1.0.2

_2026/08/31_

- `Подсказка` Этот выпуск улучшает только документацию и процесс сборки; поведение преобразования OpenCC и все 14 основных типов преобразования не изменились
- `Улучшение` Переработан README на всех 10 языках: добавлены инструкции по установке, выбору пакета и быстрой самопроверке, полный список из 33 методов сценариев, ответы на частые вопросы и сведения о разрешениях и безопасности
- `Улучшение` Инструкции центра плагинов теперь создаются из того же локализованного источника JSON, что README и CHANGELOG, поэтому все документы Android синхронизируются из единого источника
- `Улучшение` Проверка документации усилена и подключена к GitHub Actions с автоматическим обнаружением различий структуры между языками, рассинхронизации созданных файлов, потерянных артефактов, несовпадения версий и оставшихся заполнителей
- `Улучшение` Добавлен ROADMAP.md с проверяемыми списками этапов для документации, инженерной инфраструктуры, возможностей преобразования и развития среды выполнения
- `Улучшение` Конфигурация Gradle перенесена на `org.autojs.build.platform-versions` 1.4.1, а foojay используется для автоматического выбора JDK, что упрощает и унифицирует среду сборки

##### Больше истории выпусков

* [CHANGELOG.md](https://github.com/SuperMonster003/AutoJs6-Plugin-OpenCC/blob/master/app/src/main/assets/doc/CHANGELOG-ru.md)

******

### Сборка и проверка

******

Этот раздел предназначен для разработчиков, которые хотят собрать плагин из исходного кода; обычным пользователям достаточно установить готовые APK со страницы Releases.

Сборка debug APK:

```powershell
.\gradlew.bat :app:assembleDebug
```

Запуск модульных тестов JVM и сборка APK для instrumentation-тестов:

```powershell
.\gradlew.bat :app:testDebugUnitTest :app:assembleDebugAndroidTest
```

Сборка release APK:

```powershell
.\gradlew.bat :app:assembleRelease
```

Сбор артефактов выпуска с добавлением версии, ABI и дайджеста CRC32 к имени каждого файла:

```powershell
.\gradlew.bat :app:appendDigestToReleasedFiles
```

Сборка release APK и подготовка контрольных сумм и примечаний к выпуску:

```powershell
py scripts\release\prepare_release.py
```

Проверка синхронности многоязычных источников документации и сгенерированных файлов (также контролируется CI):

```powershell
py .python\generate_markdown.py --check
```

Для сборки требуются JDK 17 или новее и Android SDK 36; версии Gradle и плагинов централизованно управляются через `version.properties` и `io.github.supermonster003.autojs6-platform-versions`.

******

### Локализация и генерация документов

******

```text
.readme/common.json
.readme/android_strings.json
.readme/lang_*.json
.readme/template_readme.md
.readme/template_plugin_instruction.md
.changelog/lang_*.json
.changelog/template_changelog.md
.python/generate_markdown.py
docs/images/screenshots/README.md
docs/images/screenshots/plugin-center-enabled.png
docs/images/screenshots/standalone-phone-light.png
docs/images/screenshots/standalone-rtl-large-dark.png
app/src/main/assets/doc/CHANGELOG-*.md
app/src/main/res/values-*/strings.xml
app/src/main/res/raw-*/plugin_instruction.md
```

`.readme/android_strings.json` является единым источником строк автономного интерфейса и ошибок службы, а языковые JSON задают README и текст центра плагинов. Изменяйте JSON в `.readme/` и `.changelog/`, затем запускайте `py .python/generate_markdown.py`; созданные `strings.xml`, `plugin_instruction.md`, README и changelog вручную не правятся. `--check` проверяет все 47 артефактов.

******

### Лицензия

******

Код проекта распространяется по лицензии [Mozilla Public License 2.0](https://github.com/SuperMonster003/AutoJs6-Plugin-OpenCC/blob/master/LICENSE). Для преобразования китайского текста напрямую используется [OpenCC](https://github.com/BYVoid/OpenCC) (Apache License 2.0); источники и лицензии встроенных OpenCC, Marisa Trie, Darts Clone и RapidJSON перечислены в [уведомлениях третьих сторон](https://github.com/SuperMonster003/AutoJs6-Plugin-OpenCC/blob/master/THIRD_PARTY_NOTICES.md).

******

### Ссылки

******

- Документация AutoJs6 OpenCC: https://docs.autojs6.com/#/opencc
- Проект AutoJs6: https://github.com/SuperMonster003/AutoJs6
- Официальный проект OpenCC: https://github.com/BYVoid/OpenCC
- Уведомления третьих сторон: https://github.com/SuperMonster003/AutoJs6-Plugin-OpenCC/blob/master/THIRD_PARTY_NOTICES.md
